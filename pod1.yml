apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: new-pod1
  name: new-pod1
  namespace: demo
spec:
  containers:
  - image: nginx
    name: new-pod1


---
apiVersion: v1
kind: Service
metadata:
  name: rest-simpleapp-svc
  namespace: demo
spec:
  type: NodePort
  selector:
    run: new-pod1
  ports:
    - port: 8088
      targetPort: 80 
      nodePort: 32000
